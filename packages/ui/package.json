{
  "name": "@peptide/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured' && exit 0",
    "test": "echo 'no tests yet' && exit 0",
    "build": "echo 'sources consumed directly' && exit 0",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@peptide/shared": "workspace:*"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.7",
    "typescript": "^5.7.3"
  }
}
