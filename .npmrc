# React Native / Metro does not support pnpm's default isolated node_modules:
# Expo packages import transitive deps (e.g. @expo/metro-runtime) they don't
# declare, which only resolve when the store is hoisted. Next.js (apps/web)
# works fine either way. See Expo monorepo + pnpm guidance.
node-linker=hoisted
